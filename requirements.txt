# HTTP client (ya estaba)
httpx>=0.27.0
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0

# Settings (ya estaba)
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
rich>=13.7.0

# Backend nuevo
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0

# Auth + security
pyjwt[crypto]>=2.8.0
passlib==1.7.4
bcrypt==4.0.1
argon2-cffi>=23.1.0   # claves de usuario (Argon2) — login DNI de la webapp
cryptography>=42.0.0

# Scheduler (para 13b)
apscheduler>=3.10.0

# Web Push (para 13c)
pywebpush>=2.0.0

# Carga por archivo de RUCs (zAlerta-10 Parte C): lectura de .xlsx
openpyxl>=3.1.0

# Almacenamiento de PDFs de deuda en GCS (zAlerta-34) + extracción de texto
google-cloud-storage>=2.16.0
pypdf>=4.2.0

tzdata